% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_analysis_md.R
\name{render_analysis_md}
\alias{render_analysis_md}
\title{Render an analysis script to Markdown}
\usage{
render_analysis_md(
  script,
  output_dir = "outputs",
  report_name = NULL,
  params = list(),
  layout = c("report", "book"),
  decimal_mark = ".",
  show_code = FALSE,
  labels = report_labels()
)
}
\arguments{
\item{script}{Path to an \code{.R} script.}

\item{output_dir}{Root folder for rendered outputs. Defaults to \code{"outputs"}.}

\item{report_name}{Optional output/report name. Defaults to the script file
name without extension.}

\item{params}{Optional named list of values exposed inside the script
environment. Each entry is available by name, and the full list is also
available as \code{params}.}

\item{layout}{Output layout. Use \code{"report"} for a standalone report folder
under \code{output_dir}, or \code{"book"} to write the markdown file into
\code{output_dir} and place assets in shared \verb{images/} and \verb{tables/} folders.}

\item{decimal_mark}{Decimal separator used by inline formatting helpers
during rendering. Use \code{"."} or \code{","}. Defaults to \code{"."}.}

\item{show_code}{Logical; if \code{TRUE}, include source code blocks. Defaults to \code{FALSE}.}

\item{labels}{A label object created with \code{\link[=report_labels]{report_labels()}}. Defaults to
English \code{Table} / \code{Figure} labels.}
}
\value{
Invisibly returns the path to the generated Markdown file.
}
\description{
Runs a plain \code{.R} analysis script and writes a plain \code{.md} report.
}
\details{
\code{render_analysis_md()} evaluates the script, captures visible console output,
saves tables and ggplot objects created with \code{\link[=md_table]{md_table()}} and \code{\link[=md_plot]{md_plot()}},
and writes a structured Markdown file into a report directory.

The script may call helper functions to write text, tables, and plots:
\itemize{
\item \code{md_table()}
\item \code{md_plot()}
\item \code{md_text()}
}

\code{birtir} currently assumes one render at a time in a normal interactive
R session. Scripts may use \code{library(...)} and can affect the current
session while rendering.
}
