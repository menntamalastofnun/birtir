% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_analysis_md.R
\name{md_text}
\alias{md_text}
\title{Format inline Markdown text with glue interpolation}
\usage{
md_text(
  text,
  ...,
  digits = 2,
  style = c("apa", "plain", "p"),
  drop_leading_zero = FALSE,
  trailing_zeros = TRUE,
  decimal_mark = NULL,
  p_threshold = 0.001,
  .envir = parent.frame()
)
}
\arguments{
\item{text}{A glue-compatible character string.}

\item{...}{Additional values passed to \code{\link[glue:glue]{glue::glue()}}.}

\item{digits}{Number of decimal places. Defaults to \code{2}.}

\item{style}{Number formatting style. Use \code{"apa"} for APA-style decimals or
\code{"plain"} for standard decimals.}

\item{drop_leading_zero}{Logical; if \code{TRUE}, values between \code{-1} and \code{1}
are formatted without a leading zero. Defaults to \code{FALSE} (as APA 7 requires
retaining leading zeros for numbers that can exceed 1, such as Means and SDs).}

\item{trailing_zeros}{Logical; if \code{TRUE}, keep a fixed number of decimals.
Defaults to \code{TRUE}.}

\item{decimal_mark}{Decimal separator to use. Choose \code{"."} or \code{","}.
Defaults to \code{"."}.}

\item{p_threshold}{Lower display threshold. Values below this are shown as
\code{"< .001"} style text. Defaults to \code{.001}.}

\item{.envir}{Environment used to evaluate glue expressions. Defaults to the
caller environment.}
}
\value{
Invisibly returns the formatted Markdown text.
}
\description{
\code{md_text()} formats inline text with \code{\link[glue:glue]{glue::glue()}} and applies lightweight
number formatting to values injected with \code{{...}} expressions.
}
\details{
When called inside \code{\link[=render_analysis_md]{render_analysis_md()}}, the formatted text is appended to
the active report as Markdown. Outside rendering, the text is printed as a
console preview and returned invisibly.

\code{md_text()} supports standard Markdown and LaTeX math formulas. Both inline
math (\verb{$y = \\beta_0 + \\beta_1 x$}) and display equations (\verb{$$\\hat\{y\} = X \\beta$$})
pass directly into generated Markdown reports.

R model formulas can be dynamically formatted into LaTeX equations using
\code{\link[=formula_to_latex]{formula_to_latex()}} inside \code{{...}} expressions:
\code{md_text("Model equation: {formula_to_latex(y ~ x + (1 | group))}")}.

Note: Since \code{md_text()} uses \code{\link[glue:glue]{glue::glue()}}, literal curly braces inside LaTeX
text strings (such as \verb{\\beta_\{0\}}) should be doubled (\verb{\\beta_\{\{0\}\}}) unless
generated via an R expression such as \code{\link[=formula_to_latex]{formula_to_latex()}}.
}
\examples{
md_text("Model fit: _R_ = {0.4567}, p {0.0234}", style = "apa")
md_text("Model equation: {formula_to_latex(y ~ x + (1 | g))}")
md_text("Inline LaTeX math: $y_i = \\\\beta_0 + \\\\beta_1 x_i + \\\\epsilon_i$")
}
