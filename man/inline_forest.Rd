% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output-helpers.R
\name{inline_forest}
\alias{inline_forest}
\title{Inline forest plot}
\usage{
inline_forest(
  estimate,
  ci_low = NULL,
  ci_high = NULL,
  labels = NULL,
  n = NULL,
  p = NULL,
  width = 12,
  digits = 2,
  title = "Coefficients",
  zero = 0,
  show_stars = TRUE
)
}
\arguments{
\item{estimate}{Numeric vector of estimates, or a data frame containing
estimate and confidence interval columns.}

\item{ci_low}{Lower confidence interval bound. Ignored when \code{estimate} is a
data frame with a recognized lower-bound column.}

\item{ci_high}{Upper confidence interval bound. Ignored when \code{estimate} is a
data frame with a recognized upper-bound column.}

\item{labels}{Optional term labels. Defaults to names of \code{estimate} or a
\code{term}, \code{variable}, \code{label}, or \code{name} column when \code{estimate} is a data
frame.}

\item{n}{Optional row counts. If supplied, a count column is printed before
the estimate.}

\item{p}{Optional p-values. If supplied, stars mark \code{p < .05}; otherwise
stars mark confidence intervals that do not include \code{zero}.}

\item{width}{Width of each inline confidence interval.}

\item{digits}{Number of decimal places for estimates and intervals.}

\item{title}{Printed title.}

\item{zero}{Reference value for significance and zero-line display.}

\item{show_stars}{Logical; if \code{TRUE}, mark rows with stars using p-values
when supplied or confidence intervals that do not include \code{zero}.}
}
\value{
A \code{birtir_inline_forest} object.
}
\description{
Inline forest plot
}
\examples{
inline_forest(
  data.frame(
    term = c("x1", "x2", "g"),
    estimate = c(0.31, 0.18, 0.94),
    ci_low = c(0.12, 0.05, 0.41),
    ci_high = c(0.50, 0.31, 1.47)
  )
)
}
