% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_analysis_md.R
\name{md_table}
\alias{md_table}
\title{Format a table for Markdown reports}
\usage{
md_table(x, caption = NULL, filename = NULL, digits = NULL)
}
\arguments{
\item{x}{A data frame or table-like object.}

\item{caption}{Optional caption text.}

\item{filename}{Optional file stem used during rendering. The \code{.md}
extension is added automatically.}

\item{digits}{Optional number of digits for numeric columns.}
}
\value{
Invisibly returns the saved table path during rendering, or the
Markdown table text outside rendering.
}
\description{
When called inside \code{\link[=render_analysis_md]{render_analysis_md()}}, the table is written into the
active report and saved under the report's \verb{tables/} directory.
}
\details{
When called outside a render context, the table is printed as a pipe-style
Markdown preview in the console.

During rendering, filenames are saved into the active report's \verb{tables/}
directory. Outside rendering, \code{md_table()} is a lightweight preview helper.
}
