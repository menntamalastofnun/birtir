% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula-helpers.R
\name{formula_to_latex}
\alias{formula_to_latex}
\title{Convert a formula to a LaTeX equation}
\usage{
formula_to_latex(
  formula,
  subscript = TRUE,
  error_term = TRUE,
  notation = c("expanded", "matrix"),
  family = "gaussian",
  link = NULL
)
}
\arguments{
\item{formula}{An R formula.}

\item{subscript}{Logical; if \code{TRUE}, add \verb{_i} to the outcome and terms.}

\item{error_term}{Logical; if \code{TRUE}, add an error term to the equation.}

\item{notation}{Equation notation. \code{"expanded"} gives a readable
observation-level equation. \code{"matrix"} gives compact model-matrix notation.}

\item{family}{Model family. Currently used to decide whether to print a
Gaussian residual-error equation or a link-function equation.}

\item{link}{Optional link-function name. If \code{NULL}, a common default is used
for known families.}
}
\value{
A character string containing a LaTeX equation.
}
\description{
\code{formula_to_latex()} turns an R formula into a compact LaTeX equation for
reports, notes, and model-planning output.
}
\details{
Mixed-effect formulas follow lme4/brms-style syntax such as \code{(1 | group)},
\code{(1 + x | group)}, and \code{(x || group)}. In lme4 terminology these are
random-effect terms; in brms terminology they are group-level or varying
effects. The expanded equation prints the linear predictor. Correlation
structure, including the difference between \code{|} and \code{||}, belongs to the
random-effect covariance model and is represented in matrix notation by
\verb{B \\sim N(0, \\Sigma)}.
}
\examples{
formula_to_latex(x ~ 1)
formula_to_latex(y ~ x + g)
formula_to_latex(y ~ x * g)
formula_to_latex(y ~ x + (1 | group))
formula_to_latex(y ~ x + (1 + x | group))
formula_to_latex(y ~ x + (x || group))
formula_to_latex(y ~ x + (1 | subject) + (1 | item))
formula_to_latex(y ~ x + (1 | group), notation = "matrix")
formula_to_latex(y ~ x + (1 | group), family = "binomial")
}
