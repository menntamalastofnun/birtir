# reiknir Archive

These notes were copied from the former `reiknir` package when its
statistical-description reporting layer was merged into `birtir`.

They are kept as design history and planning context. The active package code
now lives in `birtir/R/`.
